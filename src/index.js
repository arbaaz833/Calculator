import React from "react";
import ReactDOM from "react-dom";
import NestedCom from "./app";

ReactDOM.render(
  <React.StrictMode>
    <NestedCom />
  </React.StrictMode>,
  document.getElementById("root")
);
